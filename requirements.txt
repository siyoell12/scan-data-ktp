# KTP Checker Application Requirements
# Core dependencies
# tkinter is built-in with Python standard library - no installation needed

# External packages to install
requests>=2.25.1  # For HTTP requests
reportlab>=4.4.3  # For PDF generation

# Installation command:
# pip install requests reportlab
# Note: tkinter comes pre-installed with Python
